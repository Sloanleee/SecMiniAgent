# dependency_audit
Description: Inspect dependency files for review targets.

Look for dependency manifests such as `requirements.txt`, `pyproject.toml`, `package.json`, and lock files. In this prototype, provide local review guidance and identify files that should be checked by dedicated audit tools.
